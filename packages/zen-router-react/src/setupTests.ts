// Import cleanup functions for React Testing Library (No longer needed via direct import)
// import '@testing-library/react/cleanup-after-each';
// Vitest with jsdom environment usually handles cleanup automatically.
// If needed, import { cleanup } from '@testing-library/react'; and call afterEach(cleanup);

// Add any other global setup needed for React tests in jsdom
// For example, mocking specific browser APIs if necessary