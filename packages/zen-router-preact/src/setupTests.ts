// Add any global setup needed for Preact tests in jsdom
// For example, mocking specific browser APIs if necessary

// Preact testing library cleanup is usually handled automatically by vitest + jsdom
// If needed: import { cleanup } from '@testing-library/preact'; afterEach(cleanup);