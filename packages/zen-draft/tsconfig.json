{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
    // No 'composite' or 'references' here for pnpm workspaces + tsup
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
